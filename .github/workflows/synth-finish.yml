name: Synth hardware reference finishing
on:
  push:
    branches: [82-synth-reference-finish]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: synth-finish-${{ github.ref }}
  cancel-in-progress: true
jobs:
  reference:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      OPENBLAS_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with: {persist-credentials: false}
      - name: Install numerical tools
        run: |
          mkdir -p build/finish
          sudo apt-get update > build/finish/dependencies.log 2>&1
          sudo apt-get install -y --no-install-recommends faust g++ python3-numpy python3-scipy python3-soundfile python3-matplotlib >> build/finish/dependencies.log 2>&1
      - name: Acquire identified hardware descriptors
        run: /usr/bin/python3 tools/modules/synth_finish_materials.py --out build/finish/materials
      - name: Inspect remaining producer-linked hardware archives
        run: /usr/bin/python3 tools/modules/synth_extra_materials.py --out build/finish/extra-materials
      - name: Oscillator and hardware comparisons
        run: /usr/bin/python3 tools/modules/synth_finish_driver.py --out build/finish/comparisons
      - name: Export self-contained Faust scripts and verify identical audio
        run: /usr/bin/python3 tools/modules/synth_script_exports.py --out build/finish/exports
      - name: Full preserved original synth regression
        run: /usr/bin/python3 tools/modules/synth_batch.py --out build/finish/legacy
      - name: Unit tests
        run: /usr/bin/python3 -m unittest discover -s tests -v
      - name: Standard build
        run: PATH=/usr/bin:/bin:$PATH bash scripts/build.sh
      - name: Retain exact source and evidence
        if: always()
        run: |
          mkdir -p build/finish
          git rev-parse HEAD > build/finish/commit.txt
          git archive --format=tar HEAD > build/finish/source.tar
          faust --version > build/finish/faust-version.txt
          g++ --version > build/finish/cxx-version.txt
          find build/finish -name render -type f -delete
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: synth-finish-${{ github.sha }}
          path: build/finish/
          retention-days: 14
          if-no-files-found: error
